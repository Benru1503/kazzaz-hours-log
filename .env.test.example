# E2E Test Credentials
# Create 2 accounts in your app, then fill these in.
# Student: register normally through the app
# Admin: register, then run in Supabase SQL editor:
#   UPDATE profiles SET role = 'admin' WHERE id = '<the-user-uuid>';

TEST_USER_EMAIL=student@test.com
TEST_USER_PASSWORD=test123456
TEST_ADMIN_EMAIL=admin@test.com
TEST_ADMIN_PASSWORD=admin123456

# Optional: test against your deployed site instead of localhost
# TEST_BASE_URL=https://your-app.vercel.app
